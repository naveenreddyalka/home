package ok.demo.tree;

public class result{
    public node root;
    public boolean  isLca;
    public result(node root, boolean isLca) {
        super();
        this.root = root;
        this.isLca = isLca;
    }
}
