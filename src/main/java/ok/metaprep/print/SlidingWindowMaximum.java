package ok.metaprep.print;

public class SlidingWindowMaximum {
  /*public static int[] findMaxSlidingWindow(int[] nums, int w) {
    if(nums==null || nums.length==0) return new int[0];
    int[] result = new int[nums.length-w+1];
    LinkedList<Integer> window = new LinkedList<>();
    int maxInW = Integer.MIN_VALUE;
    for(int i=0; i< nums.length-w;i++){
      if(i==0){
        for(int j=0;j<w;j++){
          window.addLast(nums[j]);
          maxInW = Math.max(maxInW,window.getLast());
          result[i] = maxInW;
        }
      }else{
        window

      }
    }

    return new int[0];
  }*/
}
