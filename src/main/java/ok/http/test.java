package ok.http;

import java.io.File;
import java.io.FileWriter;
import java.time.LocalDate;
import java.util.Arrays;
import java.util.LinkedHashMap;
import java.util.LinkedHashSet;
import java.util.Set;
import java.util.function.Function;
import java.util.function.Predicate;
import java.util.stream.Stream;

public abstract class test implements shape{
    
    private  String ex = "";
    
    public static void main(String[] args) {

        //Arrays.asList("red","green","blue").spliterator().;
        
      /*  Spliterator<String> namesSpliterator = names;
        .map(s -> !s.startsWith("b")).forEach(System.out::println);
       Predicate<String> l = s -> s.length()<2;
       Predicate<String> e = Predicate.isEqual("car");
        Function<Boolean,String> a = i -> Boolean.toString(i);
        Function<String,Boolean> b = s -> Boolean.parseBoolean(s);
        System.out.println( a.apply(true));
        System.out.println( );
        //FileWriter f = new FileWriter(new File(""));
        //Stream.of('2','1').forEach(f::write);
        
       // shape s = new qd();
       // System.out.println((tri)s ); 
       /* Set<String> set = new LinkedHashSet<String>();
        set.add("3");
        set.add("1");
        set.add("3");
        set.add("2");
        set.add("3");
        set.add("1");
        
        set.forEach(s -> System.out.println(s) );*/
  }
}
